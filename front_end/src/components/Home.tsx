import React from 'react';

interface Props {
    // Add props if needed
}

const Home: React.FC<Props> = () => {
    return (
        <div>
            <h1>Home Page</h1>
        </div>
    );
};

export default Home;