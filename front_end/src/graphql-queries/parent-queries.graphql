mutation SignupParent($data: CreateParentInput!) {
    signupParent(data: $data) {
        parentId
        firstName
        lastName
        userId
        profile {
            parentProfileId
            address
            biography
            city
            profilePictureUrl
            state
            __typename
        }
        __typename
    }
}